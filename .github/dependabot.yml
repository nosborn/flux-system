---
version: 2
updates:
  - package-ecosystem: docker
    directories:
      - apps/**/resources
    schedule:
      interval: weekly
    ignore:
      - dependency-name: "*"
        update-types:
          - version-update:semver-major
