---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nextcloud
spec:
  parentRefs:
    - kind: Gateway
      name: traefik
      namespace: kube-system
      sectionName: websecure
  rules:
    - backendRefs:
        - name: nextcloud
          port: 80
